# OpenSolar machine configuration — generated once at install time.
# Edit freely: the installer never overwrites this file on re-run or upgrade.
SOLAR_HOME={{SOLAR_HOME}}
CLAUDE_DIR={{CLAUDE_DIR}}
SOLAR_OS={{OS_KIND}}
SOLAR_COMPONENTS={{SELECTED_COMPONENTS}}
SOLAR_DB_PATH={{SOLAR_DB}}
