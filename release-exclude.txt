# PROPOSED orphan-cut exclude list — OWNER TO REVIEW + RATIFY before any cut.
# Consumed by: scripts/release-cut.sh --exclude-file release-exclude.txt
#
# These are parked, NON-INSTALLED files that still carry personal/persona tokens,
# maintainer-only release workflow notes, or excised-system design narratives.
# CLAUDE.md is NOT here: per owner decision it is scrubbed personal-data-free and
# KEPT as public contributor docs. Each was confirmed NOT in the shipped allowlists
# (kernel/base-rules.txt, base-hooks.txt, base-agents.txt), so excluding it
# per-file drops NO installer-required file. Exclude-vs-scrub is your call PER
# FILE — e.g. you may prefer to keep a SCRUBBED CLAUDE.md as public contributor
# docs rather than exclude it.
#
# NOT decided here (separate, larger owner decision): the architectural-name
# residue (solar-farm/gstack/brain-router/... that the gate tolerates
# allowlist-aware) and the broader parked dev corpus (the 128 parked agents,
# the other design reports, the ~70 unshipped hooks) you may also want dropped
# for a cleaner "day 100" public tree.

# --- release-engineering artifacts (must not ship; this file's own entries
# --- contain token-bearing FILENAMES, so it excludes itself) ---
release-exclude.txt

# --- root trigger docs (CLAUDE.md is scrubbed + KEPT, not excluded) ---
DESIGN_WORKLOG.md
SPRINTS-HIGHLIGHTS.md
TRIGGERS.md

# --- docs/ design reports (dev artifacts, not user docs) ---
docs/CLAUDE_USAGE_REPORT.md
docs/CAPSULE_ARCHITECTURE.md
docs/MEMORY_ISOLATION_PROBLEM.md
docs/MEMORY_LOSS_ANALYSIS.md
docs/ONTOLOGY_V2_DESIGN.md
docs/SOLAR_DATA_AGENT_DESIGN.md
docs/SOLAR_DATA_AGENT_DESIGN_V2.md
docs/SOLAR_TECHNICAL_REPORT_OUTLINE.md
docs/SOLAR_TECHNICAL_REPORT_V2.md
docs/COMMUNITY_NEURAL_NETWORK_DESIGN.md
docs/DISTRIBUTED_DEPLOYMENT_ANALYSIS.md
docs/SOLAR_RESOURCE_INVENTORY.md
docs/SOLAR_UNIQUENESS_ANALYSIS.md
docs/RELEASE-CHECKLIST.md

# --- parked rules (confirmed NOT in kernel/base-rules.txt) ---
rules/delegate-first.md
rules/delegate-insight-to-xiaoai.md
rules/delegate-to-xiaoai.md
rules/mempalace-diary.md
rules/solar-farm.md
rules/sprint-rhythm.md

# --- unshipped hooks (confirmed NOT in kernel/base-hooks.txt) ---
hooks/memory-auto-updater.sh
hooks/whisper-hook-v2.sh

# --- harness development notes (not installed/runtime documentation) ---
harness/BUG5-ROOT-CAUSE.md
harness/TODO-next-session.md
harness/needs-improvement.md

# --- transient runtime/orchestration state (a specific task's .pm artifacts:
# --- PRD, task DAG, handoffs, eval results — NOT release content. v1.0.0-rc.3
# --- shipped zero of these; they leaked in via the consolidation base.) ---
harness/.pm

# --- legacy/internal LaunchAgent plists ---
# The shipped daemons component uses templates/daemons/solar-daemon.*.template.
# These static com.solar.* plists belong to parked/internal jobs or legacy
# installers and are not required by components.d/daemons.
core/evolver/com.solar.evolver.plist
core/evolver/com.solar.native-evolver.plist
core/evolver/com.solar.task-executor.plist
core/ontology/com.solar.ontology-reflector.plist
deploy/com.solar.daemon.plist
deploy/com.solar.message-executor.plist
deploy/com.solar.security.plist
deploy/launchd/com.solar.resource.scores.plist
deploy/launchd/com.solar.security.access.plist
deploy/launchd/com.solar.security.daily.plist
deploy/launchd/com.solar.security.quota.plist
deploy/launchd/com.solar.security.system.plist
harness/com.solar.ai-influence-daily-digest.plist
harness/com.solar.github-trends-digest.plist
harness/com.solar.mineru-idle.plist
harness/com.solar.youtube-influence-digest.plist
harness/launchd/com.solar.tech-hotspot-radar.daemon.plist
