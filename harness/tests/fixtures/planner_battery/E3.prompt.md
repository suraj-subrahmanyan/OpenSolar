---
case_id: E3
scenario: existing_module_refactor
expected_node_count_range: [4, 6]
expected_capsule_families:
  - cap.requirement-compiler-planner
  - cap.requirement-compiler-implementation
  - cap.requirement-compiler-verification
deterministic_gate_plausible: true
deterministic_gate_examples:
  - python3 -m pytest workspace/tests -q
notes:
  - A good plan inventories current behavior before changing the module.
  - Refactor nodes should keep public behavior stable and pair changes with regression tests.
---

Refactor an existing Python module that parses a task graph JSON file and
returns a dependency order. Keep the public function name and return shape
unchanged, but split validation, cycle detection, and ordering into clearer
helpers. Add regression tests for a valid diamond dependency graph, a dangling
dependency, and a cycle.
