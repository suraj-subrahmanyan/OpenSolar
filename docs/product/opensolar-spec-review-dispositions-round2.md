# Spec Review Dispositions — adversarial review round 2 (implementation diffs)

**Date:** 2026-07-06 · Reviewer: fresh session (Opus 4.8 max), diffs-vs-code with ▶EXECUTED probes
against the real 22-capsule registry. **Outcome: 13/13 findings accepted** (3 High FIX, 1 escalation
resolved by owner-mandate reasoning, rest FIX/ACCEPT). The compile gate was passable by hanging and
cross-provider plans — caught before any live run. Fix assignments at bottom.

| # | Sev | Disposition | Resolution |
|---|---|---|---|
| **F1** empty stage∩policy intersection skips the provider filter | High | **FIX (Lane 1 fix-round)** | Empty intersection ⇒ `ROUTE_UNRESOLVABLE` compile error, never "resolve all". Regression test: RSI (openai stages) under anthropic-only policy must fail compile. Fixes the falsy-empty-set short-circuit at `workflow_contract.py:406-428`; shared by preflight (see F13). |
| **F2** planner-controlled `node_kind` (decoy `.py` or explicit field) bypasses the v7 guard | High | **FIX (Lane 1 fix-round)** | Authority correction, Sihao-faithful: **the bound capsule is the node-kind authority.** If the resolved capsule is audit/research-kind, the node is artifact/analysis regardless of declared `node_kind` or decoy write_scope entries; `patch_diff` obligations legal only when capsule kind = code AND write_scope has a code target. Planner-declared `node_kind` becomes a suggestion that can only *narrow*, never escalate to `code`. Tests: v7 + decoy-`helper.py` and v7 + explicit `node_kind:"code"` must BOTH reject. |
| **F3** no acyclicity / depends_on-existence checks on the planner path | High | **FIX (Lane 1 fix-round)** | Reuse `_check_acyclic` + add dep-existence in `validate_plan`; new codes `GRAPH_CYCLIC`, `DEP_NOT_FOUND`. Until merged, R2's anti-hang claim is explicitly not in force on the generic path (R7's bounded-wait backstop is Lane 3). |
| **F4** Lane 0 under-delivered: teardown/preflight hooks + watchdog is-terminal gate unwired; Lane 0.5 is dead code | Med-High | **ACCEPT — deferral expired; wiring is now due (Lane 0 branch owner)** | The deferral was deliberate and documented ("until Lane 0.5 defines the module interface" — commit `394a7dac`); the interface now exists, so Lane 0 PR-3 wires: teardown hook in wrapper/stop path, `preflight-run` stub at intake, watchdog `is-terminal` gate in `coordinator-watchdog.sh`. **M1 decision (escalated by reviewer): resolve the active sid at the watchdog call site; if none resolves, fail-open to global watchdog behavior for v1** — per-run respawn-gating only; AC-R7.4 wording annotated accordingly. |
| **F13** preflight compiles the contract against its embedded policy, not the run policy | Med | **FIX (Lane 0.5 fix-round)** | Pass the run provider policy into `compile_checks` from `run_preflight.py:396` (meaningful once F1 lands). One-line + a disagreement regression test (preflight and compiler must agree on the same stage). |
| **F6** `SOLAR_DEMO_REPORT_MODE=1` env-gate matches ANY text | Med | **FIX (Lane 1 fix-round; escalation resolved)** | Rule: **env_gates may constrain a match, never constitute one** — a trigger fires only on explicit marker OR requirement-compiler type; env gates are additional conjuncts. This honors the non-requirement (no demo-mode conditional as a routing *sufficient* condition) without breaking the demo driver (whose prompts contain the markers anyway). Runtime stub needs no scrub once gates can't fire alone. |
| **F5** F-CLASS-17 scenario is a tautology w.r.t. its class; 08 is a proxy | Med | **FIX (Lane 2 fix-round)** | Add `result_has_model_route` (and, post-Lane-3, route-record) assertions to 17's `expect`; recatalog 08 as `partial` until the human-review cascade is exercisable. Also refresh the stale catalog: classes 1/4/5/7 flip to verified-by-Lane-1's suite, 22 to verified-by-Lane-0.5's P1.6, on the integrated tree. |
| **F7** route facts written by operatord, outside the `set_node_status` choke point | Med | **ACCEPT-WITH-PLAN — design amended (this doc's companion edit)** | Target design §1.4 now specifies **two ledger hooks**: `set_node_status` (status transitions) AND `operator_runtime.write_result` (+ envelope-write) emitting `kind: route_record` at the operatord seam — AC-R5.1's killed-run guarantee is unachievable from the scheduler side alone. Lane 3 implements both. The baked `status:"pending"` in `instantiate` is noted as a projection-reconciled initial value. |
| **F8** Lane 0's un-gated config edits vs its own "no diff flag-off" gate | Med | **ACCEPT — contradiction resolved by re-scoping the clause (owner mandate)** | The reviewer is right that the two clauses conflict, and the resolution is intentional: **bit-preservation applies to flag-gated *code paths*; shipped-default *config* changes are deliberate product changes** — they ARE the spine (mandate: working > faithful; R8). Plan §2 Lane 0 gate wording amended; `test_spine_registry.py` correctly enforces the new defaults. Anyone needing legacy behavior re-enables operators in their local config (gating, not deletion, preserved). |
| **F9** R2(c) is lexical; `workdir/` alias re-admits the F-051 shape | Low-Med | **ACCEPT — documented limit** | Correct and known: the compile check narrows, the **manifest (Lane 3) is the real guarantee** for declared-vs-actual roots. Requirement text already routes this to R6; a doc note added to the design. |
| **F12** one malformed contract file breaks routing for ALL requests | Low | **FIX (Lane 1 fix-round)** | Router skips the malformed contract (logged), continues matching others; test with a poisoned registry dir. |
| **F10** Lane 1 shipped no spec-mismatches doc despite the largest deviations | Low | **FIX (Lane 1 fix-round)** | Write `docs/product/lane1-spec-mismatches.md` capturing the RSI capsule/task_type remap (scout=knowledge-extraction only, etc.) — currently buried in a contract note. |
| **F11** F-CLASS-17 green path timing-dependent (CI flake) | Low | **ACCEPT** | Noted; widen settle window if it ever flakes in CI. |

**Honest positives recorded:** both shell stubs flag-off-clean (the round-1 question answered); router-CLI↔stub exit-code contract consistent; Lane 2's command-backend seam choice validated; Lane 1 compile checks run against the real registry; scenarios 12/19/20/27/28 genuinely discriminating.

## Fix-round assignments (all deterministic, no live runs)

- **Lane 1 fix-round** (one session, `contract/lane1-compiler`): F1, F2, F3, F6, F12, F10. Each fix red-first (the reviewer's ▶EXECUTED probes are the red tests, verbatim).
- **Lane 0.5 fix-round** (small, `contract/lane05-supervision`): F13.
- **Lane 2 fix-round** (small, `contract/lane2-harness`): F5 + catalog refresh against the integrated tree.
- **Lane 0 PR-3** (this lineage): F4 wiring (teardown hook, preflight stub, watchdog is-terminal gate, M1 fail-open rule).
- **Lane 3** (unchanged owner): F7's dual-hook ledger design.
- **Round-3 review:** re-run the F1/F2/F3 probes verbatim after the Lane 1 fix-round lands — they must all reject.
