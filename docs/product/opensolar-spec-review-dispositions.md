# Spec Review Dispositions — adversarial review round 1

**Date:** 2026-07-06 · Reviewer: fresh Claude agent (context-free, code-cross-checked against
`integration/rc8-runtime-mode-contract`) · Review contract: implementation plan §6 checklist.
**Outcome: 11/11 findings ACCEPTED (2 with scoping).** No finding rejected. The two S1s are
design-defect-grade and were caught before any code existed — the review step did its job.

| # | Sev | Verdict | Resolution (and where it landed) |
|---|---|---|---|
| **3.1** ledger has no choke point (`set_node_status` :2359, `_mark_graph_node` :3848, `doctor_graph` :2871, inline `reviewing` :7902 all write status directly) | S1 | **ACCEPT** — the reviewer is right that the five named functions are callers, not the primitive | R4/design §1.4 rewritten: the choke point is **`set_node_status` itself** (single interception, flag-gated), `_mark_graph_node` and the audited inline writers route through it, and **`doctor_graph` is explicitly neutralized on the contracted path** (its writes become `gate_consumable=false` ledger records, never direct status). Design §4's "explicitly unchanged" re-scoped to "no rewrite; narrow flag-gated interceptions at named primitives are in scope." AC-R4.3's property test now enumerates the full writer surface (C4 list) as its grep/assert set. |
| **C1+C2** `invalid_deepdive_dag_variant` guard doesn't exist on the branch; `dag_variant` is a closed enum (`short\|standard\|parallel_spec\|parallel_delivery\|research`) consumed by two gate-backfill switches (graph_scheduler :1194-1216, codex_pm_router :1234-1270) | S1 | **ACCEPT** | Contract identity moves to a **new top-level key `workflow_contract_id`**; `dag_variant` keeps its enum semantics (contracts emit a legal enum value for gate backfill — `research` for the research contracts). The guard keys off `workflow_contract_id` and is acknowledged as **net-new** dispatcher code (small, flag-gated), not a port. Requirements AC-R1.1 and design §1.1/1.2 amended. |
| **2.1** compile+preflight green can still wedge at D5 `on_capacity_unavailable: wait` (busy singleton, F-08) | S2 | **ACCEPT** | R2's promise scoped to *admission-mismatch* hangs. The capacity hang is owned by R7: **new AC-R7.5** — `wait` is provably bounded by the stage's `result_timeout_sec`, expiry produces the wedge classification, scenario-asserted (busy-singleton scenario must show bounded wait + classification, not a hang). Note: the Lane-0 second-evaluator decision and deterministic gates on most stages shrink the exposure, but the bound is the guarantee. |
| **6.1** lane file collisions (`solar-harness.sh` ×3 lanes, scheduler ×2, dispatcher ×2); `run_preflight.py` and `plan_validator.py` have no owning lane | S2 | **ACCEPT** | Plan §2 amended with a **serialized-files rule**: `solar-harness.sh` edits land once, via Lane 0, as thin stubs calling lane-owned modules; `graph_scheduler.py`/`graph_node_dispatcher.py` edits are **owned by Lane 3 exclusively** (Lane 1's guard and Lane 0's dependency-policy change are scheduled as Lane-3 items). `plan_validator.py` → Lane 1; `run_preflight.py` → Lane 0.5 (renamed "supervision + preflight"). |
| **5.1** route-record payload (provider/model/operator/backend/exit) has no storage field in ledger or manifest | S2 | **ACCEPT** | Ledger gains a first-class **`kind: route_record`** with payload `{provider, model, operator_id, backend, exit_code, started_at, finished_at}`, appended per stage execution (design §1.4; AC-R5.1 now names it). Secondary accepted too: obligations gain an explicit **`proof_kind`** discriminator (`artifact_presence \| patch_proof \| gate`) so node-kind legality checks never string-match `field=="patch_diff"`; contracts may declare optional per-stage `terminal_states`. Recorded as schema v1.1 deltas. |
| **1.1** F-CLASS-11 orphaned (no AC, no scenario); design §5 "all 30" contradicts the plan's 28-item catalog | S3 | **ACCEPT** | Scenario 11 added to the catalog: *parseable-but-stale closeout must be FAILED by the content gate* — i.e., the scenario asserts the true-positive behavior stays true (the gate's job), while builder quality itself remains the quality track (coverage matrix verdict unchanged: PARTIAL-BY-DESIGN, now with a guarding scenario). Catalog arithmetic fixed: 29 scenarios + F-CLASS-24 explicitly delegated to Lane 6's install rung. Design §5 wording corrected. F-CLASS-08's "partly" also resolved: AC-R7.5 is now the owning AC for the busy/absent misclassification bound. |
| **7.1** plan-artifact relabel ungated · **7.2** `DEPENDENCY_BLOCK_STATUSES` is a process-global; "one line" can't be per-path | S3 | **ACCEPT** | Relabel (`suggested_operator_id`, `host_type`) gated behind `SOLAR_PRODUCT_MODE` — flag-off emits legacy fields. Human-review blocking becomes a **per-node policy consult** in the skip-propagation loop (node carries `on_human_review` from its contract; absent ⇒ legacy global behavior). Moved from Lane 0 to Lane 3 (it edits the scheduler core, per the serialized-files rule). "One-line" claim retracted. |
| **9.1** hermetic P1.5 gate hides an external-mirror dep (DeepDive source absent from repo), real-daemon teardown, and real-clock timeouts | S3 | **ACCEPT** (with credit noted: reviewer confirmed no hidden LLM/quota dep) | (a) Lane 4 step 1 is now **vendor the DeepDive source into the repo** (commit the port + parity report) so CI is self-contained; (b) the teardown scenario moves to an explicitly **non-hermetic tier "P1.6 — real-daemon integration"** (spawns real processes, still no models/network); (c) clock injection: R7 timeouts are already env-tunable knobs (`SOLAR_*_TIMEOUT_SEC*` family) — scenarios set them to seconds; this is now stated as the scenario schema's timeout-injection mechanism. |
| **4.1/4.2** fake-operator seam can't express the 18 ms mid-draft race (F-15) or real process death (F-22) | S3 | **ACCEPT — limits stated** | F-15: the scenario covers the *fix mechanism* (producer-completion gate makes sub-mtime timing irrelevant); the historical race itself is covered by the preserved v8 evidence replay at P1. F-22: real-process proof lives in P1.6 (see 9.1b); the P1.5 scenario is documented as logic-level coverage of the registry decision path. |
| **10.1** smallest mechanical P3 cut would drop Lanes 2/3 → discovery-by-live-run + hollow pass | S4 | **ACCEPT** | Plan §3 gains the reviewer's gate verbatim: **"P3 counts as evidence only if the Lane 2 catalog is green AND Lane 3 verdict-content + provenance rules are active."** Lane 5 and the manifest's dashboard consumers remain the only safe cuts for a bare P3. |
| **8.1** `warn_and_continue` on all research contracts downgrades a baseline evidence gate (mandate tension) | S4 | **ACCEPT — reviewer's own recommendation adopted** | `warn_and_continue` is retained **only** on the explicitly-unattended demo contract (`research.deepdive.rsi_demo`). General research contracts (`research.deepdive.v1`, `research.resource_radar`) get `block_dependents` with a **bounded human-wait then classified escalation** (never a silent wedge — R7 semantics). Schema example updated for Resource Radar R7. |

## Cross-check items (C3, C4)

- **C3** (DeepDive source absent from the branch): true and known — resolved by 9.1a (vendor first).
- **C4** (status-writer surface under-named): folded into 3.1's resolution; the C4 list *is* AC-R4.3's audit set.

## What this round proves about the process

The reviewer found exactly the two things an author reliably can't: that the design's structural
centerpiece (R4) was specified against the wrong seam (callers, not the primitive), and that a key
integration point was named from the *original* repo's shape rather than this branch's
(`dag_variant` enum). Both are now fixed on paper for the cost of one review. Remaining follow-up
for round 2 (optional, after Lane 1+3 code exists): re-run items 2, 3, and 7 of the checklist
against the actual diffs.
