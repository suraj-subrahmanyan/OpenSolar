# Agent install protocol → see INSTALL.md

The agent-driven install/self-check protocol has moved into the canonical
install guide. See **[INSTALL.md](INSTALL.md)** — section *Installing via an
AI coding agent* — for the up-to-date protocol (report before each command,
no `sudo`, stop on first failure, verify with `solar doctor --json`).
