# Kernel base hooks — the kernel-critical hooks installed to
# ~/.claude/solar/hooks/, all forbidden-token-clean. Kept tight: the
# scrubbed intent-engine-hook (UserPromptSubmit keystone) and its sole
# dependency hook-logger. Additional hooks ride with owning components and
# are registered incrementally via per-component hooks.json; the
# settings-merge mechanism supports that without changing this payload model.
intent-engine-hook
hook-logger
session-resume-hook
pre-compact-anchor
state-auto-updater
session-end-save
solar-stop-reminder
state-read-tracker
context-monitor
auto-favorites-extract
sma-session-start-preload
pre-bash
pre-edit
post-edit
quality-gate
task-guard
