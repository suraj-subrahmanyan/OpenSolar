# Kernel base agents — the active @Agent line, installed to
# ~/.claude/solar/agents/. The other ~128 agents ship only with the
# agents-extra component (off by default).
dev
qa
test
write
pm
researcher
