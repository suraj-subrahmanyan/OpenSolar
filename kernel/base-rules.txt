# Kernel base rules — installed to ~/.claude/solar/rules/ (lazy-loaded).
# General-discipline rules with zero forbidden tokens. Rules whose purpose is
# an excised/optional component (delegate-*, solar-farm, solar-protocol,
# call-niuma, multi-expert, niuma-acceptance, master-brain, mempalace-diary,
# tvs-rendering, task-recommendation, sprint-rhythm) are PARKED: they remain in
# the repo rules/ for contributor visibility and future components, but are not
# installed by the base kernel.
00-core-laws
01-three-core-laws
analysis-template
atlas-repair-protocol
coding-compact
coding-edge-cases
constraint-verification
cortex-first
design-completeness
infrastructure-first-check
instruction-following
intent-engine
karpathy-coding
no-mock
no-tmp-artifacts
output-persist
state-machine-first
state-persistence
task-create-protocol
