## codex-bridge (已安装，默认关闭)

可选的基于文件的交接桥，安装到 `~/.solar/codex-bridge`，供外部编码代理协作。

- 默认关闭，零运行时依赖。
- 协议见 `~/.solar/codex-bridge/CODEX-PROTOCOL.md`。
- Solar 的引擎仍是 Claude Code；codex-bridge 是可选附加项，不改变默认运行时。
