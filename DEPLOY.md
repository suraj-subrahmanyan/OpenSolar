# Deployment → see INSTALL.md

The install and deployment guide is now canonical in
**[INSTALL.md](INSTALL.md)** — covering requirements, interactive and
unattended installs, component selection, the installed layout, Windows
(WSL2), verification, and the `solar` lifecycle commands.

For uninstalling, see [`docs/UNINSTALL.md`](docs/UNINSTALL.md).
